# SynapseGuild Starter Baseline

A welcoming CLI baseline to verify readiness for SynapseGuild projects.

## Quick Start

```bash
# Run the readiness CLI
npm start

# Run the verification tests
npm test
```

## Verification Criteria
- Emits `READY` status with a welcoming guild message.
- Exits with status code `0`.
